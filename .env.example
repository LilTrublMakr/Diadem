# Diadem Application
DIADEM_PORT=3900

# MariaDB Database (Diadem Internal — used by Docker)
MARIADB_ROOT_PASSWORD=changeme_root
MARIADB_DATABASE=diadem
MARIADB_USER=diadem
MARIADB_PASSWORD=changeme

# Dragonite Database (read access only — for historical stats_workers data)
DRAGONITE_DB_HOST=127.0.0.1
DRAGONITE_DB_PORT=3306
DRAGONITE_DB_NAME=dragonite
DRAGONITE_DB_USER=
DRAGONITE_DB_PASSWORD=
